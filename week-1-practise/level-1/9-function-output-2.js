function s(w1, w2) {
    return w1.concat(w2);
}

var result = s('code', 'your');
console.log(result);

// Answer these questions:
// - What does this program do? Run it and see, or Google some of the methods used ot understand them. Creates a new string with w1 and w2 values
// - How many parameters does the function take? 2
// - What is the function name? s
// - Where is the function called? with what parameters? s is called with par 'code' and 'your' and is assigned to result. 
// - What does the function return? codeyour
// - How can this function be improved? take a 3rd parm, use + instead.